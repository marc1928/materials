spring:
  application:
   name: InscriptionEtAuthentification
  datasource:
    url: jdbc:mysql://localhost:3306/authentication
    username: root
    password: marc
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
#  cloud:
#    consul:
#      host: localhost
#      port: 8500
#      discovery:
#        enabled: true
#        register: false
#        service-name: authentification
#        instance-id: ${spring.application.name}:${random.value}
#        prefer-ip-address: true
server:
  port: 8083

